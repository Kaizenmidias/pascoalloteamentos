import { Link } from '@inertiajs/react';
import AdminLayout from '../../Components/Layout/AdminLayout';

const Card = ({ label, value, hint, href }) => <Link href={href} className="rounded-xl border border-gray-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><p className="text-xs font-medium uppercase tracking-wide text-gray-500">{label}</p><strong className="mt-2 block text-3xl font-medium text-gray-900">{value}</strong><span className="mt-2 block text-xs text-gray-500">{hint}</span></Link>;
export default function Dashboard({ counts, statusCounts, recentLeads = [], recentPosts = [] }) {
    const cards=[['Imóveis',counts.properties,'Cadastros independentes','/admin/properties'],['Condomínios',counts.condominiums,'Empreendimentos cadastrados','/admin/condominiums'],['Loteamentos',counts.subdivisions,'Empreendimentos cadastrados','/admin/subdivisions'],['Leads',counts.leads,`${counts.newLeads} novos contatos`,'/admin/leads'],['Postagens',counts.posts,'Conteúdos do blog','/admin/blog/posts'],['Páginas',counts.pages,'Conteúdo institucional','/admin/pages']];
    return <AdminLayout title="Início"><div className="space-y-7"><div><p className="text-sm text-gray-500">Bem-vindo(a) de volta. Aqui está o resumo da Pascoal Loteamentos.</p></div><div className="grid gap-4 tablet:grid-cols-2 desktop:grid-cols-3 xl:grid-cols-6">{cards.map(([label,value,hint,href])=><Card key={label} label={label} value={value} hint={hint} href={href}/>)}</div><div className="grid gap-5 tablet:grid-cols-3">{[['Publicados',statusCounts.published,'text-emerald-700'],['Rascunhos',statusCounts.draft,'text-amber-700'],['Destaques',statusCounts.featured,'text-brand']].map(([label,value,color])=><div key={label} className="rounded-xl border border-gray-200 bg-white p-5 shadow-sm"><p className="text-xs font-medium uppercase text-gray-500">{label}</p><strong className={`mt-2 block text-3xl font-medium ${color}`}>{value}</strong></div>)}</div><div className="grid gap-6 desktop:grid-cols-2"><section className="rounded-xl border border-gray-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-gray-100 p-5"><h2 className="font-medium text-gray-900">Leads recentes</h2><Link href="/admin/leads" className="text-xs font-medium uppercase text-brand">Ver todos</Link></div><div className="divide-y divide-gray-100">{recentLeads.map(lead=><div key={lead.id} className="flex items-center justify-between gap-4 p-4"><div><p className="text-sm font-medium text-gray-900">{lead.name}</p><p className="text-xs text-gray-500">{lead.property?.title||lead.condominium?.title||lead.subdivision?.title||'Contato geral'}</p></div><span className="rounded-full bg-gray-100 px-3 py-1 text-xs text-gray-600">{lead.status}</span></div>)}</div></section><section className="rounded-xl border border-gray-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-gray-100 p-5"><h2 className="font-medium text-gray-900">Postagens recentes</h2><Link href="/admin/blog/posts" className="text-xs font-medium uppercase text-brand">Ver todas</Link></div><div className="divide-y divide-gray-100">{recentPosts.map(post=><Link key={post.id} href={`/admin/blog/posts/${post.slug}/edit`} className="flex items-center justify-between gap-4 p-4 hover:bg-gray-50"><p className="text-sm font-medium text-gray-900">{post.title}</p><span className="text-xs text-gray-500">{post.status}</span></Link>)}</div></section></div></div></AdminLayout>;
}

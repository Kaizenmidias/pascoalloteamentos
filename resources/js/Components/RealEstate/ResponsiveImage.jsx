export default function ResponsiveImage({ src, srcSet, sizes = '(max-width: 767px) 100vw, 50vw', alt = '', className = '', loading = 'lazy', width, height }) { if (!src) return <div className={`bg-line ${className}`} role="img" aria-label={alt || 'Imagem não disponível'} />; return <img src={src} srcSet={srcSet} sizes={sizes} alt={alt} loading={loading} width={width} height={height} className={className} />; }

export default function FeatureChoices({features=[],selected=[],onChange}){function toggle(id){onChange(selected.includes(id)?selected.filter(value=>value!==id):[...selected,id])}return <fieldset><legend className="admin-label">Características</legend><div className="grid gap-2 tablet:grid-cols-2">{features.map(feature=><label key={feature.id} className="flex gap-2 rounded-xl border border-line p-3"><input type="checkbox" checked={selected.includes(feature.id)} onChange={()=>toggle(feature.id)}/>{feature.name}</label>)}</div></fieldset>}

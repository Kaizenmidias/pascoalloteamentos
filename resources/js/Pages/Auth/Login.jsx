import { Head, Link, useForm } from '@inertiajs/react';

export default function Login() {
    const { data, setData, post, processing, errors } = useForm({ login: '', password: '', remember: false });
    const submit = (event) => { event.preventDefault(); post('/login'); };
    return <main className="relative grid min-h-screen place-items-center overflow-hidden bg-ink p-5"><Head title="Acesso administrativo" /><img src="/reference-assets/hero-home.jpg" alt="" className="absolute inset-0 h-full w-full object-cover opacity-25" /><div className="absolute inset-0 bg-black/55" /><form onSubmit={submit} className="relative z-10 w-full max-w-md rounded-card bg-white p-8 shadow-card"><Link href="/" className="mb-7 block"><img src="/reference-assets/logo.png" alt="Pascoal Loteamentos" className="mx-auto w-48" /></Link><h1 className="text-center text-2xl font-normal text-ink">Acesso administrativo</h1><p className="mt-2 text-center text-sm text-muted">Entre com seu usuário ou e-mail.</p><label className="mt-7 block"><span className="admin-label">Usuário ou e-mail</span><input className="admin-input" value={data.login} onChange={(event) => setData('login', event.target.value)} autoFocus autoComplete="username" />{errors.login && <span className="mt-1 block text-xs text-red-700">{errors.login}</span>}</label><label className="mt-4 block"><span className="admin-label">Senha</span><input className="admin-input" type="password" value={data.password} onChange={(event) => setData('password', event.target.value)} autoComplete="current-password" />{errors.password && <span className="mt-1 block text-xs text-red-700">{errors.password}</span>}</label><label className="mt-4 flex gap-2 text-sm"><input type="checkbox" checked={data.remember} onChange={(event) => setData('remember', event.target.checked)} /> Lembrar acesso</label><button type="submit" disabled={processing} className="brand-button mt-6 w-full">Entrar</button></form></main>;
}

import { Link } from '@inertiajs/react';
import PublicLayout from '../../../Components/Layout/PublicLayout';
import SeoHead from '../../../Components/SEO/SeoHead';
import Container from '../../../Components/UI/Container';

export default function Show({ post, related = [] }) {
    return <PublicLayout><SeoHead title={post.seo?.title || post.title} description={post.seo?.description || post.excerpt} /><section className="relative flex min-h-[400px] items-center px-5 pt-20 text-center text-white"><img src="/reference-assets/about-plans.jpg" alt="" className="absolute inset-0 h-full w-full object-cover" /><div className="red-overlay absolute inset-0" /><div className="relative z-10 mx-auto max-w-5xl"><p className="text-xs font-bold uppercase">{post.categories?.[0]?.name || 'Mercado imobiliário'}</p><h1 className="mt-4 text-[clamp(2rem,4.5vw,3.7rem)] font-normal leading-tight">{post.title}</h1></div></section><Container className="relative z-10 -mt-16 pb-[var(--section-space)]"><article className="mx-auto max-w-5xl rounded-card bg-white p-5 shadow-card tablet:p-8"><img src={post.featured_media?.url || '/reference-assets/blog-city.jpg'} alt="" className="aspect-[16/9] w-full rounded-card object-cover" /><p className="mt-5 text-xs text-muted">Por {post.author?.name || 'Pascoal Loteamentos'} · {new Date(post.published_at).toLocaleDateString('pt-BR')}</p><h2 className="mt-4 text-2xl font-normal text-brand">{post.title}</h2><div className="mt-4 whitespace-pre-line text-sm leading-7 text-muted">{post.content}</div></article>{related.length > 0 && <section className="mx-auto mt-12 max-w-5xl"><h2 className="section-title">Leia também</h2><div className="mt-6 grid gap-6 tablet:grid-cols-3">{related.map((item) => <Link key={item.id} href={`/blog/${item.slug}`} className="overflow-hidden rounded-card border border-line bg-white"><img src={item.featured_media?.url || '/reference-assets/blog-city.jpg'} alt="" className="aspect-[16/9] w-full object-cover" /><div className="p-4"><h3 className="text-sm font-medium text-ink">{item.title}</h3><span className="mt-3 block text-[.68rem] font-bold uppercase text-brand">Leia mais</span></div></Link>)}</div></section>}</Container></PublicLayout>;
}

export default function Map({ latitude, longitude, title = 'Localização' }) { if (latitude == null || longitude == null) return <div className="grid min-h-80 place-items-center rounded-card bg-surface text-sm text-muted">Localização disponível sob consulta.</div>; const url = `https://www.openstreetmap.org/export/embed.html?bbox=${longitude - .01}%2C${latitude - .01}%2C${longitude + .01}%2C${latitude + .01}&layer=mapnik&marker=${latitude}%2C${longitude}`; return <iframe title={title} src={url} loading="lazy" className="min-h-80 w-full rounded-card border-0" referrerPolicy="no-referrer" />; }

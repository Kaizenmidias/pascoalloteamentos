export default function SelectField({label,error,options=[],valueKey='id',labelKey='name',placeholder='Selecione',...props}){return <label className="block"><span className="admin-label">{label}</span><select className="admin-input" {...props}><option value="">{placeholder}</option>{options.map(option=><option key={option[valueKey]} value={option[valueKey]}>{option[labelKey]}{option.state?.code?` / ${option.state.code}`:''}</option>)}</select>{error&&<span className="mt-1 block text-xs text-red-700">{error}</span>}</label>}

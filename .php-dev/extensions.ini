; Local CLI extensions required to install and test this application.
; Production must enable the equivalent extensions in its own PHP runtime.
extension=fileinfo
extension=mbstring
extension=openssl
extension=pdo_mysql
extension=pdo_sqlite
extension=sqlite3

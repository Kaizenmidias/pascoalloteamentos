import { Link } from '@inertiajs/react';
export default function AdminTable({ headers, items, renderRow, empty='Nenhum registro encontrado.' }) { return <div className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr>{headers.map(header=><th key={header} className="px-5 py-4 font-medium">{header}</th>)}</tr></thead><tbody className="divide-y divide-gray-100">{items.data?.length?items.data.map(renderRow):<tr><td colSpan={headers.length} className="px-5 py-12 text-center text-gray-500">{empty}</td></tr>}</tbody></table></div>{items.links?.length>3&&<nav className="flex flex-wrap justify-center gap-2 border-t border-gray-100 p-4">{items.links.map((link,index)=><Link key={index} href={link.url||'#'} preserveScroll className={`rounded-lg border px-3 py-2 text-xs ${link.active?'border-brand bg-brand text-white':'border-gray-200 text-gray-600'}`} dangerouslySetInnerHTML={{__html:link.label}}/>)}</nav>}</div>; }
